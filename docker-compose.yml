services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: tsotan
      POSTGRES_PASSWORD: tsotan
      POSTGRES_DB: tsotan
    ports:
      - "5432:5432"
    volumes:
      - tsotan_pg:/var/lib/postgresql/data

volumes:
  tsotan_pg:
